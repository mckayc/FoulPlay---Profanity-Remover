"""Single source of truth for the app version, shown in the UI (window
title + Home page) so users can tell which build they're running. Keep in
sync with MyAppVersion in packaging/foulplay.iss.
"""

from __future__ import annotations

APP_VERSION = "0.4.0"
